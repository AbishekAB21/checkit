
import 'package:flutter/material.dart';

class TaskDetailScreenComponent extends StatelessWidget {
  const TaskDetailScreenComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}